# CodegraphChat.Api

HTTP API for topic chat over a pre-indexed Codegraph project.

- Port: `http://localhost:5091`
- Endpoints: `/api/health`, `/api/session`, `/api/chat`

Start:

```powershell
cd ..\scripts
.\Start-CodegraphChat.ps1 -Force
```
