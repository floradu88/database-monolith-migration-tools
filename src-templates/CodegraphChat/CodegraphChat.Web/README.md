# CodegraphChat.Web

Angular 18 chat UI for Codegraph topic questions.

Proxies `/api` → `http://localhost:5091`.

```powershell
cd ..\scripts
.\Start-CodegraphChatWeb.ps1
```

Open http://localhost:4201.
