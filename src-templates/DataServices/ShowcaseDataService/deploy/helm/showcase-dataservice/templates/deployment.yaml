apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "showcase.fullname" . }}-blue
  labels:
    app.kubernetes.io/name: showcase-dataservice
    app.kubernetes.io/component: blue
spec:
  replicas: {{ .Values.replicaCountBlue }}
  selector:
    matchLabels:
      app.kubernetes.io/name: showcase-dataservice
      app.kubernetes.io/component: blue
  template:
    metadata:
      labels:
        app.kubernetes.io/name: showcase-dataservice
        app.kubernetes.io/component: blue
    spec:
      containers:
        - name: api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          env:
            - name: MigrationRouting__Slot
              value: {{ .Values.blue.slot | quote }}
            - name: MigrationRouting__DefaultRoute
              value: {{ .Values.blue.defaultRoute | quote }}
            - name: Database__Schema
              value: {{ .Values.database.schema | quote }}
            - name: Database__Owned__Provider
              value: {{ .Values.database.provider | quote }}
            - name: Database__SourceFacade__Provider
              value: {{ .Values.database.sourceProvider | quote }}
            - name: Database__Owned__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecretName }}
                  key: {{ .Values.ownedConnectionKey }}
            - name: Database__SourceFacade__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecretName }}
                  key: {{ .Values.sourceConnectionKey }}
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "showcase.fullname" . }}-green
  labels:
    app.kubernetes.io/name: showcase-dataservice
    app.kubernetes.io/component: green
spec:
  replicas: {{ .Values.replicaCountGreen }}
  selector:
    matchLabels:
      app.kubernetes.io/name: showcase-dataservice
      app.kubernetes.io/component: green
  template:
    metadata:
      labels:
        app.kubernetes.io/name: showcase-dataservice
        app.kubernetes.io/component: green
    spec:
      containers:
        - name: api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          env:
            - name: MigrationRouting__Slot
              value: {{ .Values.green.slot | quote }}
            - name: MigrationRouting__DefaultRoute
              value: {{ .Values.green.defaultRoute | quote }}
            - name: Database__Schema
              value: {{ .Values.database.schema | quote }}
            - name: Database__Owned__Provider
              value: {{ .Values.database.provider | quote }}
            - name: Database__SourceFacade__Provider
              value: {{ .Values.database.sourceProvider | quote }}
            - name: Database__Owned__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecretName }}
                  key: {{ .Values.ownedConnectionKey }}
            - name: Database__SourceFacade__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.existingSecretName }}
                  key: {{ .Values.sourceConnectionKey }}
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
